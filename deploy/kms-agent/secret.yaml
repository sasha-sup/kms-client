apiVersion: v1
kind: Secret
metadata:
  name: kms-agent-secret
  namespace: kms-system
type: Opaque
stringData:
  # PLACEHOLDER — replace with the actual pre-shared HMAC key before deploying.
  hmac-key: "CHANGE_ME_BEFORE_DEPLOY"
